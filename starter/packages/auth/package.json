{
  "name": "@starter/auth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The Better Auth instance, shared by the Next.js proxy gate and the Fastify service so ONE session cookie is validated at BOTH gates (DECISIONS.md #10). Ships TypeScript source; consumers transpile (Next) or run it natively (Node 24+).",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "db:init": "node src/migrate.ts",
    "check:types": "tsc -p tsconfig.json",
    "check:lint": "biome check .",
    "check:format": "biome format ."
  },
  "dependencies": {
    "better-auth": "1.6.23"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "6.0.3"
  }
}
