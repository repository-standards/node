# <what it was> - <topic>

<!-- Copy to docs/discovery/<topic>/<YYYY-MM-DD>-<what-it-was>.md, one file per source.
     The header table is read by grep, so keep the field names and the order. The body is
     the essence: who said what mattered. The raw recording or thread stays in its own tool
     and is linked, never pasted here. -->

| | |
| --- | --- |
| **Kind** | `meeting` |
| **Date** | `<YYYY-MM-DD>` |
| **Present** | roles, first names at most |
| **Purpose** | why this happened - the question it was called to answer |
| **Touches** | `<topic>`, `<other-topic>` |
| **Raw** | link to the recording, thread or mail |
| **Outcome** | `none yet` |

<!-- Kind is one of: meeting, call, mail, thread, ticket, document, note. It is a fixed
     vocabulary rather than free text so that "was that decided in a meeting or in a mail
     thread" is answerable across a whole dossier without reading it.

     Purpose is the field people skip and later need most. Six months on, what a session
     was FOR is the thing nobody reconstructs from notes of what was said in it.

     Touches lists the subjects this material bears on, beyond the dossier it is filed in.
     A dossier is one topic, but one session rarely is: a call about invoicing lands in
     invoicing and touches pricing, tax and refunds. This is the axis you search when you
     remember a conversation happened but not what it was filed under.

     Outcome is what this entry produced, filled in when it produces it: the record, spec
     or backlog row it became. `none yet` is an honest and common value. -->

## What was said

Attributable points - who said what mattered, and whether it was settled:

- **<who>:** <the constraint, number, promise or decision, stated as they stated it>
- **<who>:** <what was argued but NOT settled - say so explicitly; an open question that
  reads as a decision is worse than no note at all>

## Explained here

<!-- Optional, and the reason this section is named rather than left to the section above:
     the durable value of a long session is often an EXPLANATION - how something works, why
     a constraint exists - which is neither a decision nor behaviour, so neither a record
     nor a spec will ever hold it. Written down here it stays findable; left out, it is
     what somebody re-researches next year. Keep it short and link the source. -->
