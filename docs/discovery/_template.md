# <Topic> - discovery dossier

<!-- Copy to docs/discovery/<topic>/README.md. One folder per topic; one file per
     source beside this one, from _entry-template.md, named <date>-<what-it-was>.md.
     Never normative: where this and a spec disagree, the spec has already won. -->

**Summary.** What this topic is, and - once it matures - what came out of it, linked.

**Last reconciled:** `<date> (specs/<capability> @ <commit>)`

<!-- The stamp bounds what gets ASKED, not what gets read. The spec skills raise only
     entries newer than this, plus anything still new/open, so nobody is questioned twice
     about a settled call. Anyone reading to understand - answering a question, researching
     something that was explained once already - reads the whole dossier, stamp or no
     stamp. Update it when you fold the dossier into a spec; never edit it by hand to
     silence a question. -->

## Entries

| Date | Kind | Source | Touches | State | Outcome |
|---|---|---|---|---|---|
| `<YYYY-MM-DD>` | `meeting` | `<file>.md` - one line on what it was | `<other-topic>` | `new` | `none yet` |

<!-- The row mirrors the entry's own header table, so the dossier can be scanned without
     opening every file. Kind and Touches are what make "which of these was a mail" and
     "which of these touched tax" answerable here rather than by reading all of them.

     State moves new -> folded-into-spec | superseded-by: <record> | open. Outcome names
     what the entry actually produced (`ADR-012`, `specs/invoicing`, `backlog#41`) - the
     state says an entry was consumed, the outcome says by what. -->

## Contradictions to resolve

| What disagrees | Source A | Source B |
|---|---|---|
| | | |

<!-- Raised by discovery-digest when a new entry disagrees with an earlier one.
     Each row names both sources so the disagreement can be taken to a person. -->
