# Adoption intake - <repo>

> What Step 0 of `align-to-standards` measured and asked, before any greenfield,
> brownfield or stack work proceeded (ADR-042). Updated in place on every
> re-entry - a check-up, an update, a later wave - never recreated.

## State measured

- **self-verify drift at intake:** {{DRIFT_COUNT}} (or "no `.standards-version` -
  unaligned brownfield" / "no repo yet - greenfield").
- **Lifecycle and policy signals found:** deprecated/archived/frozen markers, an
  AI/agent policy file and what it said, a fork-or-moved signal - or "none found".
- **Existing-knowledge sources offered:** what the user pointed at (a tracker, a
  wiki, a `DECISIONS.md`, nothing) and whether it was reachable and quotable.

## Question round

- **Intent:** {{start new / bring existing to the standard / assessment only /
  update the pin / a check-up / stays as-is}}.
- **Technology:** {{detected and confirmed - or "undecided, deferred" on a
  greenfield}}; Layer 2 consent given or declined.
- **Appetite:** {{one focused PR / a programme of waves}}.
- **Profile:** {{core / scale}} - why.
- **Tracked-work location:** {{in-repo / a tracker / both, bridged}}.
- **Plan-only or execute:** {{...}}.
- **Evidence:** {{send an anonymised excerpt / send nothing}}.

## Re-entry log

Each later Step 0 pass appends one line here rather than rewriting the round
above: date, what changed (the drift number, a route taken), and whether any
answer above no longer holds.

- {{YYYY-MM-DD}}: first intake, as above.
